{
    "global_settings": {
        "description": "全局设置",
        "type": "object",
        "items": {
            "default_openlist_url": {
                "description": "OpenList API 地址（机器人访问）",
                "type": "string",
                "default": "",
                "hint": "AstrBot 用来访问 OpenList API 的地址。普通部署直接填公网地址；Docker/内网部署可填 http://openlist:5244 这类内网地址。"
            },
            "public_openlist_url": {
                "description": "对外下载地址（可选）",
                "type": "string",
                "default": "",
                "hint": "只有 API 地址是内网地址、但发给用户的下载链接需要公网访问时填写。普通部署请留空，留空时使用 API 地址生成链接。"
            },
            "default_username": {
                "description": "默认用户名",
                "type": "string",
                "default": "",
                "hint": "留空表示匿名访问"
            },
            "default_password": {
                "description": "默认密码",
                "type": "string",
                "default": "",
                "hint": "用户名为空时可忽略"
            },
            "default_token": {
                "description": "默认访问Token",
                "type": "string",
                "default": "",
                "hint": "可选，优先级高于用户名密码"
            },
            "fixed_base_directory": {
                "description": "路径前缀修正（高级，可选）",
                "type": "string",
                "default": "",
                "hint": "仅当 OpenList 列表路径与 /d 或 /api/fs/link 所需真实路径不一致时填写。例如列表看到 /file.txt，但真实路径应为 /夸克/file.txt，则填 /夸克。普通部署请留空。"
            },
            "max_display_files": {
                "description": "最大显示文件数",
                "type": "int",
                "default": 20,
                "slider": {
                    "min": 1,
                    "max": 100,
                    "step": 1
                }
            },
            "allowed_extensions": {
                "description": "允许的文件扩展名",
                "type": "text",
                "default": "",
                "hint": "用逗号分隔，如: .txt,.pdf,.jpg。留空表示不限制；设置后会限制下载、取链接、预览和上传。"
            },
            "max_preview_size": {
                "description": "最大预览文件大小(MB)",
                "type": "int",
                "default": 10
            },
            "text_preview_length": {
                "description": "文本文件预览长度",
                "type": "int",
                "slider": {
                    "min": 100,
                    "max": 4000,
                    "step": 100
                },
                "default": 1000
            },
            "enable_cache": {
                "description": "启用文件列表缓存",
                "type": "bool",
                "default": true
            },
            "cache_duration": {
                "description": "缓存有效期(秒)",
                "type": "int",
                "default": 300,
                "minimum": 60,
                "maximum": 3600
            },
            "max_download_size": {
                "description": "最大下载文件大小(MB)",
                "type": "int",
                "slider": {
                    "min": 0,
                    "max": 500,
                    "step": 1
                },
                "default": 50,
                "hint": "0 表示不限制。"
            },
            "max_upload_size": {
                "description": "最大上传文件大小(MB)",
                "type": "int",
                "slider": {
                    "min": 0,
                    "max": 10240,
                    "step": 1
                },
                "default": 100,
                "hint": "0 表示不限制。"
            },
            "upload_retry_attempts": {
                "description": "上传单文件尝试次数",
                "type": "int",
                "default": 3,
                "hint": "普通上传失败时最多尝试次数，包含首次尝试。"
            },
            "upload_retry_delay": {
                "description": "上传重试间隔(秒)",
                "type": "int",
                "default": 5,
                "hint": "同一文件两次上传尝试之间的等待时间。"
            },
            "upload_chunk_size_mb": {
                "description": "上传分块大小(MB)",
                "type": "int",
                "default": 4,
                "hint": "本地/URL中转上传时每次读取的块大小。"
            },
            "upload_progress_step_mb": {
                "description": "上传进度日志间隔(MB)",
                "type": "int",
                "default": 64,
                "hint": "仅 debug_transfer_logging 开启时生效。"
            },
            "upstream_connect_timeout": {
                "description": "上游URL连接超时(秒)",
                "type": "int",
                "default": 60
            },
            "upstream_read_timeout": {
                "description": "上游URL无数据超时(秒)",
                "type": "int",
                "default": 180
            },
            "openlist_connect_timeout": {
                "description": "OpenList上传连接超时(秒)",
                "type": "int",
                "default": 30
            },
            "openlist_upload_response_timeout": {
                "description": "OpenList上传响应超时(秒)",
                "type": "int",
                "default": 3000
            },
            "debug_transfer_logging": {
                "description": "启用传输诊断日志",
                "type": "bool",
                "default": false,
                "hint": "开启后输出上传/下载进度、连接信息和 DNS 解析等详细日志。"
            },
            "require_user_auth": {
                "description": "要求用户认证",
                "type": "bool",
                "default": true,
                "hint": "启用后每个用户需要独立配置"
            },
            "default_submit_path": {
                "description": "投稿模式默认根路径",
                "type": "string",
                "default": "",
                "hint": "开启投稿隔离：填写 OpenList 目录（如 /submit），开启后每个用户只能在其个人子文件夹（/submit/<QQ号>）内 素材 列表/下载/上传 等操作，无法退出或访问其他目录。留空表示关闭投稿隔离。"
            }
        }
    }
}
